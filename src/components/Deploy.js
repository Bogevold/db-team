import React from 'react';

const Deploy = (deploy) => {
//  console.log(db);
  return (
    <div className="deploy">
      {/*{servere.server.navn}*/}
      Navn:{deploy.server.navn}
    </div>
  )
};

export default Deploy;